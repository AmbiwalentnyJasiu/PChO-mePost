
@if (User.Identity.IsAuthenticated)
{
    <form method="post" class="form-inline" asp-page="/Account/Logout">
        Welcome @User.Identity.Name
        <button type="submit" class="ml-2 btn btn-outline-primary">Logout</button>
    </form>
}
else
{
    <a class="btn btn-outline-primary" asp-page="/Account/Login">Login</a>
    <a class="btn btn-outline-primary" asp-page="./register/register">Register</a>
}